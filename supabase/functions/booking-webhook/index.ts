// Deployed Edge Function: custom booking API webhook.
// Runtime version deployed in Supabase validates x-routsify-secret when WEBHOOK_SECRET is set
// and upserts bookings by organization_id + external_booking_id + event_type.
